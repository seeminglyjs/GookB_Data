 ※ 문자열 파싱
	문자열에서 원하는 형시의 데이터를 추출한다.
	Wrapper클래스에는 정적메소드로 제공되는 parseXXX() 형식의 메소드가 있다.
	각 데이터 타입에 맞는 형태의 문자열에서 데이터를 추출한다.

** Character에는 파싱 메소드가 없다.
	charAt()를 사용하면 된다.

*Boolean 클래스의 parseBoolean은 "true"를 외에는 모두 "false" 로 파싱한다.