 + TB : logintb
  - COLUMNS
    id VARCHAR2(50) primary key
    pw VARCHAR2(50) not null
    email VARCHAR2(200)
    phone VARCHAR2(50)


 + 테이블 생성 쿼리

DESC logintb;
DROP TABLE logintb;
CREATE TABLE logintb (
    id VARCHAR2(50) PRIMARY KEY,
    pw VARCHAR2(50) NOT NULL,
    email VARCHAR2(200),
    phone VARCHAR2(50)
);
SELECT * FROM logintb;